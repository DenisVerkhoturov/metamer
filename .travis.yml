language: java
jdk: oraclejdk11

env:
  - GRADLE_OPTS=-Dorg.gradle.daemon=false

matrix:
  include:
    - os: linux
      language: java

    - os: windows
      language: shell
      env: JDK=j11;

    - os: osx
      language: java

install:
  - ./.travis/install.sh

script:
  - gradlew check
